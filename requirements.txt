# --- Streamlit app core ---
streamlit>=1.50

# --- Core YOLOv5 deps (adapted for server) ---
# Use HEADLESS OpenCV to avoid libGL errors on cloud/server
opencv-python-headless>=4.10.0
numpy>=1.24
Pillow
PyYAML>=5.3
scipy>=1.4
tqdm>=4.41
matplotlib>=3.2
pandas
seaborn

# --- PyTorch CPU wheels (Cloud is CPU-only) ---
# Let pip pick compatible wheels for your Python version
torch
torchvision

# --- WebRTC / video ---
streamlit-webrtc>=0.63
aiortc>=1.6
av>=10
